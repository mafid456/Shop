apiVersion: apps/v1
kind: Deployment
metadata:
  name: Ecom-deploy
spec:
  replicas: 2
  selector:
    matchLabels:
      app: Ecom-app
  template:
    metadata:
      labels:
        app: Ecom-app
    spec:
      containers:
      - name: Ecom-container
        image: 
        ports:
        - containerPort: 80
